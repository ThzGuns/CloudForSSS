image:
  name: hashicorp/terraform:1.6
  entrypoint: [""]

stages:
  - deploy

before_script:
  - cd terraform
  - terraform --version


deploy:
  stage: deploy
  script:
    - terraform init
    - terraform validate
    - terraform apply -auto-approve
  only:
    - main
